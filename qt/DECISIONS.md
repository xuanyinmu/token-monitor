# Qt rewrite decisions

This file is the running judgment log for the C++/QML port. `src/shared` remains the spec. Choices below are made so work can continue without blocking the user.

## Bound (from the plan)

- Electron stays the shipping product. `qt/` is a parallel Windows-first port.
- Cloudflare Worker stays JS. C++ Hub speaks `docs/API.md` and reports `runtime: "native"` with **no** JS `hubBuild` hash (legacy-compatible; avoids false “redeploy Hub”).
- No Qt HttpServer / Charts, no third-party C++ libs. Discord RPC and DSH zstd stay deferred.
- Namespace `tmon`. userData is `%APPDATA%\Token Monitor\` (same as Electron), not Qt’s `Org/App` nested path.

## Scaffold

- Old `qt/` MVP sources are not compiled. A full recursive wipe of leftover `qt/src/app`, `qt/src/limits`, `qt/src/window`, and duplicate `qt/qml/*.qml` files was blocked by the environment; CMake only lists the new tree. Leftover unreferenced files are ignored.
- Three executables + `tmon_core`. `TokenMonitor` is a console subsystem on purpose so `--scan-once` can print JSON during development.
- i18n: plan asked for `.ts` via Node. This machine often has no `node` on PATH, so CMake prefers `extract-i18n.js` and falls back to `extract-i18n.ps1`. Runtime loads JSON (`:/i18n/i18n.json`), not Qt Linguist. Same five locales.

## Usage

- Watcher lives on its own `QThread` (plan: not on the collector thread). `QFileSystemWatcher` is not chokidar: we recursively register existing directories to depth 4, cap 500, skip tokscale cache dirs. Unknown paths still trigger an all-client `--today` scan.
- Subprocess: `terminate` → 4s grace → `kill`, plus a generation counter so a superseded scan cannot publish. We do not emulate the JS “unconfirmed close” second grace unless a hang shows up in practice.
- Period delta follows the JS object walk (arrays become keyed objects). Identity is guarded by `TokenMonitorDeltaCheck`.
- tokscale `sessions` / `workspaces` arrays are folded onto rows before extract (decoded path only; opaque keys stay unattributed). Project id is `sha256:` of the normalized path, matching `projectIdentity()` spirit, not tokscale’s workspace key.
- WSL: registry gate, `wsl.exe --list --running` only, `--home \\wsl$\…` on full ticks. Month/allTime for WSL are not separately scanned (same CPU reason as JS: WSL is full-tick only; we merge today’s WSL period into today and leave month/allTime for the Windows full scans plus delta). Documented limitation: a WSL-only client’s month/allTime catches up on the next full tick.
- Local parsers: Proma jsonl is summed. Qoder CN sqlite is best-effort (schema is undocumented); missing tables are a no-op, not a crash.
- `tokscale.exe` prefers userData over next-to-exe so a leftover `qt/build/tokscale.exe` cannot shadow the vendored binary.

## Limits

- `LimitsRuntime` uses concurrency 3 (`QtConcurrent` + per-call `HttpClient`), per-provider skip-if-in-flight lanes, `lastGood` retention on transient statuses, exponential backoff, and adaptive burn-rate with the JS constants (base 5m, floor 60s, samples-ahead 4, release weight 0.3). Adaptive only **shortens** the interval.
- All 24 catalog ids are dispatched. Cookie/OAuth providers that need a Chromium profile are implemented as: read the same local files/tokens Electron uses, then hit the same HTTP endpoints. We do **not** drive a browser. If the local credential is missing, status is `notConfigured`. Empty windows with `status: ok` means “account seen, quota payload not mapped yet” and is treated as a gap to fill, not success.
- Spend/balance files stay under `userData/data/` with the Electron filenames (`deepseek-balance-v2.json`, …).
- `mimoCookie` is not in `CREDENTIAL_SETTING_PATHS`; MiMo stays metadata/managed-accounts only, same as JS.

## UI

- Plan forbids a single `AppController`. `AppState` is the shell coordinator (window/tray/hotkey/settings). Rows go through `RowListModel` (`QAbstractListModel`) per breakdown. QVariantList properties remain as a QML convenience alias.
- Theme tokens copy Electron CSS (`#b7ead4`, glass 48,52,56, presets default/obsidian/porcelain). Radius 8 on inner cards; window chrome uses DWM rounded corners (SDK `DWMWCP_ROUND`) rather than pretending to be 14px CSS.
- Custom controls only (Basic style, no Material/Fusion).
- Session detail is an overlay, not a route. Dashboard is a second `QQmlApplicationEngine` window.
- Icons: reuse `assets/icons/*.svg` via qrc. Missing client marks fall back to a letter tile.
- Settings: eight sections covering the Electron `defaultSettings()` keys that a user can change without a dedicated account-picker UI. Nested profile editors (OpenRouter/OpenCode/third-party maps, Cursor account multi-select) are JSON text fields rather than the full Electron accordion — same store shape, denser editor. `showHomeLimitBars` default is **false** to match Electron.

## Hub / agent

- Standalone `TokenMonitorHub` stores `%APPDATA%\Token Monitor\data\devices.json` so a Node hub in a git checkout and the native hub do not clobber each other by accident. Widget **host** mode keeps Electron’s `hub-devices.json` in userData.
- Agent writes `agent.pid`. Widget sync-upload skips POST when that PID is alive (`OpenProcess`). Stale pid files are unlinked.
- Subscriptions: local list in `settings.json` in `local` mode; hub `PUT` with `baseUpdatedAt` / 409 in `client`/`host`.

## Chrome extras

- Tray uses `assets/icons/token-monitor.svg`.
- Floating bubble is a **second small window**, not a full-widget overlay.
- Top-edge hide: poll cursor; dock off-screen; peek on pointer at y≈0.
- Export: CSV+JSON. Diagnostics: redacted settings + last snapshot + lastError JSON next to export dir.
- Currency: if `currencyRates` lacks the active code, probe `https://open.er-api.com/v6/latest/USD` once per process (no extra dependency). Failure leaves USD.
- GitHub latest-release check unchanged. Start-at-login via HKCU Run.

## Explicitly not in this cycle (unchanged)

- Discord RPC, DSH zstd, Worker rewrite, macOS vibrancy/WidgetKit, new C++ libraries.

## UI parity vs Electron (2026-09-16)

The first Qt shell was a generic dashboard (emoji footer, always-on window buttons, letter tiles). Electron chrome is specific; this pass copies it rather than inventing a native-Qt layout.

- **Shell:** 340×650, padding `12 14 14`, radius 8 on Windows, **no extra 1px CSS border** (DWM rounds the window). Glass tokens match `--glass-rgb 48,52,56`, `--number #f3fbf7`, `--blue #73bdf5`.
- **Titlebar:** 30px, 16px bold title, 4px live-dot (`#5b6471` / accent glow). Status line only for error/fail/offline. Period control is a **148×30 segmented grid** (DAY/MONTH/TOTAL) with a sliding 6px-radius indicator. Pin/min/close (34×28, gap 6, 114px) **replace** the tabs on top-right hover and while settings is open — not shown at rest.
- **Total panel is global**, not Home-only: `TOTAL TOKENS` + `clamp(30–46px)` display font (Segoe UI) + `$cost`. Row lists sit under it. Full grouped number (`1,234,567`) in the hero; compact `1.2M` stays on rows.
- **Footer:** view-switcher (icon + label + chevron, max 150px) + settings/refresh hover-swap. No 9 glyph buttons, no footer token total.
- **Rows:** 10px SVG mask (tinted via `MultiEffect`), name/metrics row, then a 6px **blue** bar. Home modules use uppercase heads + jump icons and cap at 5 tools / 5 models / 4 limits.
- **Settings** is an overlay flag (`settingsOpen`), not a breakdown view, so the switcher still shows Tool/Home/…. Accordion cards stack with 12px outer radius and 15px section icons from the Electron renderer SVG set (qrc `/ui`).
- **Default breakdown** is `tool`, matching Electron `lastViewState`. Existing Qt installs that saved `home` keep home until changed.
- **Back to Home** is always shown on non-home views. Electron gates this on `homeReturnVisible` (only after leaving Home). Showing it always is more discoverable and still matches the control chrome; recorded so it can be gated later if a screenshot complains.
- **Icons:** `assets/icons/*.svg` for client marks; `src/electron/renderer/icons/{views,actions,settings}` copied into qrc `/ui` so masks stay 1:1 with Electron, not redrawn.
- **Cookie/OAuth limits, Discord RPC, DSH zstd** still deferred. Visual parity of those settings rows is layout-only.
- **Home modules** use Electron ids (`limits,tool,device,model,trends`) and the same default hide list (`tool,device`). A first Qt pass used `tools/models/heatmap`, so Home rendered blank against a real `settings.json`. Icons are colorized with a `QSvgRenderer` + `SourceIn` image provider (`image://mask/…`) because Qt SVG + `currentColor` otherwise stays black.
- **Screenshot / compare loop:** tokscale on the GUI thread blocked `--screenshot` (QTimer never fired; a hung `TokenMonitor.exe` then LNK1104-locked the binary). Screenshot mode now sets preview-only: skip collector/tray/hotkey, hydrate last `collector-anchor.json` + `history.json` (and Electron `daily-history-archive.json` if Qt history is empty), grab after 900ms. Release links as `TokenMonitorQt.exe` until the locked `TokenMonitor.exe` is gone.
- **Home limits** match Electron: account head + up to two window cells showing remaining (`N left`), not a used-meter LimitRow. Bars stay off unless `showHomeLimitBars`. Tool/model home rows show compact tokens + **share %**, not `$cost`.
- **Limits page** shows remaining meters per window, plan label, and not-configured rows as “Not signed in”, matching the Electron limits panel structure (Cursor-style stacked windows).
- **i18n qrc path:** `qt_add_resources` without `BASE` stored the catalog at `:/i18n/resources/i18n.json`, so `I18n` loaded an empty file and the switcher fell back to raw ids (`home`). Loader now accepts both aliases and CMake sets `BASE` to `resources/`.
- **Settings controls:** native Qt ComboBox/Button painted as Fusion-light on Windows; replaced with glass ComboBox/Button (sunken 5% overlay, 8px radius, `--text`).
- **Footer** includes the idle live-rate chip (`zap` + `— tok/s`) between the switcher and settings, same slot as Electron.
- **Limits snapshot** is persisted to `limits-snapshot.json` so screenshot/preview mode can paint last-known quotas without waiting on provider HTTP. Preview still starts LimitsRuntime (not tokscale) so a warm cache can refresh.

- **Activity heatmap** is a rolling-year Sunday grid (cell 9 / gap 3) with Electron's four-level blue fills and month labels; trend line is a 45-day area chart with a 2px stroke.

## UI parity iteration (2026-09-16, continued)

Visual 1:1 vs the live Electron widget (Limits / zh-CN) was still failing. Choices made so the loop can keep going:

- **CJK mojibake:** `I18n.t("views.home")` dumped as UTF-8 `涓婚〉` (classic UTF-8-as-GBK of `主页`). Disk `i18n.json` was already garbled. Cause: `extract-i18n.ps1` used `Get-Content` without `-Encoding UTF8` (Windows PowerShell ACP = GBK). CMake prefers Node, but a later PowerShell extract overwrote the catalog. Fix: UTF-8 extract on both paths, re-emit JSON from `i18n.js`, keep RCC as binary. Not a font bug — Cascadia Mono is restored as the Electron default UI font (`ui-monospace` → Cascadia Mono / Consolas); display numbers stay Segoe UI from `displayFontFamily`. Qt cannot parse CSS font stacks, so we take the first real family.
- **Cursor limits stub:** `fetchCursor` returned `ok` + empty windows whenever Cursor files existed, then `publish()` overwrote `limits-snapshot.json`. Electron's live Limits page shows Cursor Models / Other Models / Grok Bot / On-demand spend. Qt now reads tokscale `cursor-credentials.json` (desktop `state.vscdb` fallback), probes `cursor.com` with the same cookie + browser UA as `probe.js`, and maps the same windows. Empty `ok` responses do not replace a last-good payload that already has windows. Screenshot mode waits up to ~6s for a windowed row instead of grabbing at 1.8s.
- **Limits chrome:** remaining is `N% left` (not `N left`); spend is `$used / $limit` with no meter; reset line `Reset 12h 9m`; `Updated 4m ago` under the name; plan / `Not signed in` on the right. Those English strings match Electron — it does not translate them even in zh-CN.
- **Home limits empty copy** uses `home.noLimits`. Home window cells use the Electron kind-priority + max-2 slice (`homeWindows`).
- **Heatmap archive path:** Electron `sharedDataDir` is `%APPDATA%\Token Monitor`. Qt `sharedDataDir()` is `userData/data` (hub isolation). Archive is at the userData root; hydrate now unions missing days from that file even when `history.json` already has today.
- **Title collapse:** `titleIconOnly` in the shared settings is on, so Electron shows Σ at rest. Qt now honors that flag (and still collapses on overflow). Heatmap is a horizontal scroller parked on the most recent weeks (Electron’s activity strip is `overflow-x: auto` and lives off the right edge of a 340px window). Archive days are summed from `observations`, not a missing top-level `tokens` field.
- **Home list rows** are a single 4-column line (mark / name / tokens / share), 16px tall, matching Electron `.home-model-row`. The previous stacked metrics made Home ~40px taller and pushed Trend below the fold.
- **Heatmap months** use the resolved UI locale (`zh-CN` → `9月`), not hardcoded English `Apr`.
- **Settings chrome:** Electron inserts the accordion between the titlebar and TOTAL TOKENS; collapsed sections keep the current view visible underneath. Qt now does the same (settings is not a full-body replacement). Default is all sections collapsed, with muted right-side summaries (`settings.summary.*`). Collection summary prefers `clientStatus` health counts and falls back to tracked-client counts when preview hydrate has no health object. Expanded General still uses left-label / right-control rows.
- **Screenshot `--view settings`** parks the Limits breakdown underneath so the grab matches the live Electron compare surface (settings + TOTAL + Cursor windows). A normal settings toggle still keeps whatever view was showing.
- **Judgment:** do not chase DWM acrylic wash in `grabWindow` PNGs. Layout, copy, meters, fonts, and i18n stay the parity target. Token totals may differ between Qt `--screenshot` hydrate and a live Electron collector; that is data, not chrome.

## Live widget vs screenshot hydrate (2026-09-16)

The user's live Qt Limits shot (full "Token Monitor" title, English "Limits", raw `ok` / `notConfigured` meters, no DAY tabs, no tok/s) does not match the current QML tree. That layout is the old `LimitRow` page. Screenshot hydrate was already closer to Electron; live was the remaining gap.

- **Usage blocked limits:** `DeviceRuntime::start` ran a GUI-thread tokscale `fullScan` *before* `LimitsRuntime::start`. The live Limits page could sit on a stub `ok` + empty windows until the collector returned. Limits now start first; tokscale is `QTimer::singleShot(0)` after the first paint so Cursor HTTP can complete on the event loop.
- **Title Σ:** `!!app.settings.titleIconOnly` was a QVariantMap lookup. Electron stores the flag as a real boolean and defaults it on. Qt now exposes `app.titleIconOnly` from the settings object (bool / 0-1 / "true"), default **true**, same as Electron `defaultAppearance`.
- **tok/s chip:** Electron hides `#liveTokenRate` until `showLiveTokenRate === true`. The live Electron shot has it on, so the user's `settings.json` has the flag. Qt Footer now gates on `app.showLiveTokenRate` instead of always painting the idle chip (which made old vs new binaries harder to tell apart, and would mismatch a default-off install).
- **Cursor probe UA:** `HttpClient` stamped `TokenMonitor/0.57.0` *then* applied provider headers. A 200 HTML/challenge body parsed as empty JSON and published `status: ok` with no windows — the live "ok" row. Default UA is now the browser string, skipped when the caller sets `User-Agent`, HTTP/2 off, and non-JSON 200 is `unavailable` (last-good windows kept).
- **Limits copy:** remaining percents stay 0–100 like Electron `core.js`; QML `remainFrac` accepts either scale. Limits-page values stay `--text` (Electron does not redden 0% left on that page). Raw status strings never render as meter labels.
- **Judgment:** keep iterating against the *live* `TokenMonitorQt.exe` window, not only `--screenshot`. Kill leftover `TokenMonitor.exe` so an old link cannot be what the user is looking at. The 2026-09-16 live PrintWindow pair (`qt/build/compare/qt-live-limits.png` vs `electron-live-0.png`) shows the Limits chrome now matching: Σ + DAY/MONTH/TOTAL, zh-CN `额度`, Cursor Pro windows (Cursor Models / Other Models / Grok Bot / On-demand spend), `Not signed in`, zap `tok/s`. Remaining deltas that are **not** treated as chrome blockers: DWM acrylic wash (Electron 356×722 shadow frame vs Qt 340×650 client), and token totals while a long-running Electron collector is ahead of a freshly launched Qt hydrate/scan. `TokenMonitor.exe` in `qt/build` is overwritten from `TokenMonitorQt.exe` so a stale output name cannot relaunch the old LimitRow UI.

## Live Home / Limits / Tool / Trends (2026-09-16 evening)

Captured Electron by cycling the view-switcher **current** button (it advances to the next view; the chevron/long-press opens the menu). Authoritative pair: `electron-cycle-1.png` (Limits) / `electron-cycle-3.png` (Home) / `electron-cycle-4.png` (Tool) / `electron-cycle-2.png` (Trends).

Choices made after that pair:

- **Home window labels** follow Electron `homeLimitWindowLabel`: a `weekly` kind renders `home.limit.weekly` ("Weekly" in every locale), not the raw "Grok Bot" collector label. Reset copy under the cell is shown at 9px. Remaining percents stay `--text` unless `showHomeLimitBars` is on.
- **Home model marks** use `modelVendorFor` (same regex family as `usageCharts.js`). CSS swaps `.row-icon-xai` → `grok.svg` and `.row-icon-grok` → `xai.svg`; Qt maps vendor `xai` to `grok.svg`. Unrecognized models use a Σ glyph — `token-monitor.svg` is a `<text>Σ</text>` that QSvgRenderer does not paint, so HomeListRow draws the character directly.
- **Home trend** is tokens (not heatmapMetric), Catmull-Rom-ish cubic matching `smoothLinePath`, dates as `M/D`, peak meta `home.peakTokens`. Heatmap cells are rx=2 and no longer back-fill empty cost days with tokens (that made the grid denser than Electron).
- **Footer current** cycles like Electron; chevron hover/click (and 420ms long-press) opens the menu.
- **Breakdown rows** use grouped `formatNumber` (`196,151,383`) and the client/vendor bar color (Cursor `#000000`), not compact M + always-blue.
- **Trends page** is the Electron 7-bar spark + 2×2 stats, not a heatmap. `today` scopes active-time and peak to **today's** archive row (14m / today's tokens); active days and streak stay all-history. Hydrate now copies `activeTimeMs` from `daily-history-archive.json` onto existing `history.json` days.

Still open for full 1:1: Settings accordion density vs Electron, Dashboard window, DWM acrylic wash (not a chrome blocker), and Electron’s footer update pill (`↑ v0.58.0`) which Qt does not show — Qt keeps the `tok/s` chip the attached Electron target used.

## Live Limits + Home (2026-09-16 night)

Rebuilt `TokenMonitorQt.exe` and captured live PrintWindow pairs (not `--screenshot`):

- Limits: `qt/build/compare/qt-live-limits.png` vs `electron-live-limits.png` (`electron-cycle-1.png`)
- Home: `qt/build/compare/qt-live-home.png` vs `electron-live-home.png` (`electron-cycle-3.png`)

Chrome that now matches Electron on those views:

- Σ title, 148×30 DAY/MONTH/TOTAL, zh-CN footer (`额度` / `主页`), zap `tok/s`
- Cursor **Pro** windows: Cursor Models remaining, Other Models, **Grok Bot** 100% left, On-demand spend `$used / $limit`
- GLM / DeepSeek **Not signed in**
- Home 额度 cells: **Weekly** 100% left + Cursor Models remaining + Reset
- Home 模型 compact units strip trailing `.0` (`118M` not `118.0M`; suffix `M` not `m`/`k`)
- Home heatmap / 趋势 / 活跃 20 天 structure

Judgments this pass:

- **Grok Bot missing** was a Cursor sand-usage parse, not packing. Qt used `QJsonValue::toBool()` on `hasNonZeroIncludedLimit` (false for numeric/string) and a 5s POST timeout. Now uses a typed `jsonBool`, optional nested `usage` object, `includedLimit > 0` fallback, 15s timeout. Home then correctly prefers weekly Grok Bot as “Weekly” over Other Models.
- **tok/s vs update pill:** Electron hides tok/s when `#appUpdatePill` is visible (`live-token-rate-obscured`). Live Electron currently shows `↑ v0.58.0` because GitHub latest is newer than `0.57.0`. Qt now checks latest in the background, strips a leading `v`, and paints the same pill (click opens the release URL). tok/s still shows when the setting is on and no update is ready. The attached screenshot with tok/s was a no-update session, not a different footer layout.
- **Settings collection summary:** Electron reads `clientHealth` (`healthy`/`waiting`/`attention`/`unavailable`), not the legacy `clientStatus` map. Qt never produced `clientHealth`, so the accordion fell back to `追踪 N`. Qt now derives the same overall from `clientSourceRoots` + allTime tokens (detected+tokens=healthy, detected+0=review, missing=unavailable) for the `clients` CSV.
- **Settings card density:** Electron is one iOS-style grouped card (`panel 0.35`, 40px rows). Qt’s extra 0.45 slab is gone; headers are 40px.
- **Dashboard:** Electron’s Usage Dashboard is a second 920×620 frameless window (Overview/Trends, 8 stat cards, heatmap Tokens/Cost, model+tool breakdown). Qt’s stub is replaced with that chrome. Home 趋势 ↗ still goes to the Trends page; Trends ↗ / spark open the dashboard window (not `view=dashboard`, which would replace the widget). Trends daily bars are token totals for the selected range; stacked per-client history is a remaining data-shape gap (Qt `historyDays` has no `perClient`).
- **Session hover-scroll:** Electron marquees overflowing title/id on hover (240ms delay, 22ms/px). Qt `UsageRow` now does the same when `hoverScroll` is on (Session view).
- **Token totals** still diverge while both collectors run. Data, not chrome.
- **Session rows** use Electron `sessionRows.js` fields. Cost uses `$X.XX` (`formatUsd`); Electron’s compact-currency extra decimals stay out until compactMoney is ported.
- **Status** fetches the four statuspage summaries in parallel on dedicated `QThread`s. Pills use `serviceStatus.*` i18n.

- **Dashboard heatmap window:** Electron Overview uses a 12-month grid starting on the 1st of the month 11 months back. Qt Home stays a 53-week rolling year (matches the widget). Dashboard Heatmap now uses `twelveMonth`.

## Live chrome pass (2026-09-16, obvious mismatch)

The user still saw an obvious live mismatch after Limits/Home checklist items were structurally in place. Choices this pass:

- **Session msgs:** `UsageNormalize` counted `+1` per tokscale model row (`2 msgs`). Electron uses `firstNumber(row, MESSAGE_COUNT_KEYS)` then sums on merge (`18 msgs`). Qt now uses the same keys, and copies a session-array `messageCount` onto a row that has none.
- **USD under $10:** Electron `fractionDigitsFor` uses 4 decimals below $10 (`$1.9511`). Qt `formatUsd` always used 2. Now USD `>= 10` → 2 digits, else 4.
- **DWM hairline + missing shadow:** Electron acrylic does **not** `DwmExtendFrameIntoClientArea(-1)` (that path is Accent-only). Qt punched the whole client, which drops the Win11 card shadow (`340×650` vs Electron `356×722`) and washes the glass. Acrylic mode now sets `DWMWA_SYSTEMBACKDROP_TYPE` + dark mode + `DWMWA_BORDER_COLOR=NONE` + `CS_DROPSHADOW`, and only extends the frame in Accent mode.
- **Home module rhythm:** Electron `.home-panel` gap 12, `.home-module` gap 7 + padding-bottom 12 + 1px rule. Qt stacked modules with spacing 8 and the rule inside the 7px gap. Each module is now an inner column (gap 7, padding-bottom 12) plus the rule, with 12px between modules.
- **Limits title weight:** `.limit-name` is 12px regular; Qt `Font.DemiBold` is dropped.
- **Session bars:** `.session-mode .bar` is 5px / gap 5 / padding-bottom 8. `UsageRow` takes `barHeight` (Session passes 5).
- **TOTAL TOKENS** bottom padding 12 to match `.total-panel`.
- **Dashboard % clip:** breakdown row used a 86px reserve for 90px of token+percent text. Switched to `RowLayout` with a filling bar and 52/44 trailing columns.
- **Judgment:** acrylic wash is chrome when it changes the window silhouette (shadow / hairline / extra transparency). Token totals while both collectors run independently stay data, not chrome. Live captures must key Electron by process image, not the shared `"Token Monitor"` title.

- **Update pill:** `api.github.com` returned 403 (unauthenticated quota). Electron uses `https://github.com/Javis603/token-monitor/releases/latest` with `Accept: application/json` so public checks skip the API. Qt now uses that same route.

- **Footer switcher width (2026-09-16 user report):** Electron `.view-switcher` is `flex: 0 1 auto; max-width: min(150px, 52%)` (112px when live-token-rate is enabled) with `margin-right: auto`. Qt gave the switcher `Layout.fillWidth`, so it grew/shrunk with leftover row space and looked shorter than Electron. It is now content-sized with the same 150/112 cap.
- **Settings-open gear shift:** Electron `.utility-actions` stays `flex: 0 0 34px` on the right; refresh is `position: absolute; right: calc(100% + 6px)`. Qt hid the center spacer when settings opened, so the 34px slot packed left next to the (now hidden) switcher. The fill-width spacer now always stays in the row; only its chips hide.
- **MONTH/TOTAL unclickable:** Electron `.window-actions` is `pointer-events: none` until `.actions-hotspot` (28×28 at `top:-12; right:-14`, L `clip-path`) or settings-open. Qt put a 114×30 hover overlay over the whole tab cluster, so entering MONTH/TOTAL revealed pin/min/close and stole the click. Hotspot is now the L only; overlay enables after reveal (140ms hide delay, same as CSS).
- **Settings right column clipped:** `SectionBody` inner Column was `x: 11; width: parent.width` inside a `clip: true` card, so every control overflowed 11px and switches/combos lost their right edge. Inner width is `parent.width - 22`.
- **Main view list:** Electron `#viewDisplayList` (name + eye + grip, home nested) was missing. Added, including reset/show-all. `hiddenViews` / `viewDisplayOrder` no longer restart the collector.
- **Switcher stays up in settings:** Electron does not hide `.view-switcher` when settings is open (only `.tool-detail-footer`). Qt had `visible: !settingsOpen`, which dropped the control the user was comparing. Restored. Settings body uses `Layout.fillHeight` leftover instead of `maxHeight: height-180` so 货币 / 模型排序 land above TOTAL like Electron.
- **Appearance first screen:** Electron 外观 is glass System/Transparent, Windows Acrylic, live/tool/title/compact/rate toggles, unit/swap, Glass/Depth/Zoom sliders. Qt had a 3-row backdrop/theme/heatmap stub; heatmap belongs under Home. Ported the Electron first screen. `systemGlass: false` maps to DWM `DWMSBT_NONE` + opaque theme glass. `settingsInTitlebar` swaps footer primary like `.utility-actions.is-swapped`.
- **Section scroll:** Electron does **not** auto-scroll an opened accordion to the top — 常规 stays visible when 主画面 expands. Qt had started scrolling the opened section into view to fit Appearance sliders; that diverged from Electron and also broke the capture click map. Removed. Appearance sliders below the fold match Electron (user scrolls).
- **Settings descriptions:** Electron `.settings-item-desc` wraps onto a second full-width line. Qt `SettingRow` was `maximumLineCount: 1`, which truncated 默认统计范围. Now 2 lines.
- **Grok/Weekly on Home:** Electron labels `kind: weekly` as "Weekly" (Grok Bot included limit at 0% used = 100% left). Qt dropped the window when `usagePercent` was missing. Now included-limit with no percent still emits a 0%-used weekly window.
- **View-row 三:** Electron `.view-subgroup-icon` masks `settings/general.svg` on home/status/project/trends. Qt used a chevron on home only; now the same general.svg mark on those four ids.
- **Switcher chrome (follow-up):** Electron splits `.view-switcher-current` (icon+label, padding 8, `flex: 0 1 auto`) from a 24px `.view-switcher-disclosure`. Qt had been stretching the whole capsule to the 150px cap, which made 趋势/额度 look longer than Electron. It now sizes to the label and uses the same 24px chevron hit-target. Long-press 420ms opens the menu, matching `VIEW_SWITCHER_LONG_PRESS_MS`.
- **Hotspot geometry:** L arms are explicit `x/y` (top 28×12 above the tabs, right 14×28 in the 14px padding). Hover over MONTH/TOTAL must not set `hotspotHot`.
- **Settings exclusive accordion (2026-09-16 user report):** Electron `setSettingsSectionExpanded` closes every other section, and `initSettingsAnimationWrappers` turns `.settings-section-details` into a 250ms `grid-template-rows` accordion. Qt allowed multiple sections and swapped Loader height with no animation. Opening a section now sets `open` to only that id; body height/opacity animate 250ms InOutCubic.
- **Settings leftover Home:** Electron keeps Home under TOTAL when settings is open (额度/模型 in the leftover). Hiding Home and `fillHeight` on settings produced the empty well the user marked. Settings is content-sized again (capped so TOTAL+footer remain); Home always `fillHeight`.
- **DWM caption strip / right shift:** `WS_THICKFRAME` plus `setColor(transparent)` left a light DWM caption on top and a left resize frame, so chrome looked offset. `WM_NCCALCSIZE` now makes the client cover the HWND; window color is Theme.bg (`#303438`) instead of transparent; caption color matches.
- **Settings gear spin:** `IconButton` spun whenever `iconSource` was set and `active` was true, so the settings gear spun and stayed accent-green while settings was open. Only refresh sets `spinning`.
- **White strip on resize:** `DwmExtendFrameIntoClientArea(-1)` plus `Window.color: transparent` shows DWM's light frame in newly exposed pixels. Window color is `Theme.bg`. The settings slot no longer uses `layer.enabled` or `preferredWidth: root.width` (those broke fill-width and left a GPU layer stale on resize).
- **Refresh spinner:** Electron's idle refresh is the `↻` glyph; `spinner.svg` only applies with `.is-refreshing`. Qt always used spinner.svg. Idle is now `↻`; spinner + accent for ~700ms after click.
- **Combo width:** Electron `.settings-item > select` is `width: auto; max-width: 58%`. Sizing from the eliding `contentItem.implicitWidth` collapsed the control (circular). Width now comes from an unelided probe `Text.contentWidth + 48`, capped at 58% of the panel, and the popup matches the closed control.
- **MONTH range menu clicks:** The 本月/本周/近7/近30 popup was a `Popup.Item` child of the 30px tab strip, so it painted over Home but hit-testing went to the heatmap (the 0 tokens tooltip). It now parents to `Overlay.overlay` (or the window content item) and is modal without a dimmer.

## Screenshot mismatch pass (2026-09-16)

User live shots of Electron vs Qt (zh-CN). Judgments:

- **Language ComboBox:** Windows native highlight painted the selected row white. Qt ComboBox now owns popup chrome (dark glass, accent highlight, `highlight: null`) and palette so Basic style cannot fall back to the native list.
- **Settings leftover:** Electron leaves Home (额度/模型) under TOTAL when settings is open. Qt matches that: settings is content-sized, Home stays visible.
- **Settings overlap (2026-09-16 live shot):** Opening 常规 painted a rounded grouped card over TOTAL + leftover Home, with accordion summaries and heatmap continuing to the right of the card. Cause: one tall `clip:false` card (alpha 0.35, `implicitWidth` 280) overflowing the layout cell, composited beside a wider HWND. Fix: per-section cards like Electron's `.settings-collapsible-group` (`rgba(panel, 0.35/0.55)`, first/last 12px corners), a full-width clipped `settingsSlot` with `layer.enabled`, Flickable `anchors.fill`, and `SWP_FRAMECHANGED` after `WS_THICKFRAME` so QML width follows the client. Leftover 额度/模型 still sits under TOTAL; it must not show *through* or *beside* the accordion.
- **Home heatmap / trend:** Electron Home uses `rollingYearHeatmap` (12 months from the 1st of month −11) plus `clampDaily(history, 45)` over the **sparse** history series (zero days omitted), so dates can read `4/9 … 9/16` with a wave. Filling the last 45 calendar days with zeros flattened the line into a spike — that was a misread of `clampDaily`. Qt Home heatmap is `twelveMonth`; tooltip is `formatTokens` + ISO date like Electron's activity tooltip. Month labels at 340px still clip on the left (scroll to end); widening the window reveals 2月, same as Electron.
- **Collection / Limits / Subs / Sync:** Electron is `#clientDisplayList`, `#limitProviderCheckboxes`, empty-state `settings.subscriptions.emptyList` + add toggle, and three `.hub-mode-option` radios + device ID. Qt stubs (WSL toggles, limits switches, Hub-mode combo) are replaced with those lists. Drag-reorder of rows is still a gap (Electron also dropped the grip; drag is on the row).
- **Resize:** Electron `WINDOW_LIMITS` 240×140–1200×1400, resizable except desktop-pinned. Qt frameless windows need `startSystemResize` on edge handles **and** `WS_THICKFRAME` on the HWND or Aero will not size.
- **Token refresh:** Watch debounce is already 1500ms (Electron). The lag was `todayScan`/`fullScan` blocking the GUI thread, so watcher ticks queued behind tokscale. Scans now run on `QThread::create`; results apply on the GUI thread; a mid-scan watch re-arms as `pendingToday`. `todayScan` also persists `history.json`. Totals while both collectors run independently can still differ — data, not chrome.




## Fixed-range headline periods, dock animation, popups (2026-09-19)

- **本周 / 最近 7 天 / 最近 30 天 are now derived, not aliases of month.** `AppState::rebuildDerivedPeriod()` mirrors Electron `fixedPeriodRanges.derivePeriod()`: locale-aware week start (`QLocale::firstDayOfWeek`, ISO-Monday fallback), range sum over the retained daily history, per-day client/model attribution folded back into a period object, and a `[start, end]` range exposed for QML. With no history days at all the headline reads `—` / empty cost (Electron's "never silent zeroes" rule) instead of month data.
- **History day records now carry attribution.** Qt's own `history.json` days already stored `clients`/`models` but no costs; `UsageEngine::hydrateFromDisk` folds Electron `daily-history-archive.json` observations (`JSON([client, modelId])` keys) into per-day `clients`/`clientCosts`/`models`/`modelCosts` and `activeTimeMs`. Maps are never injected into a day that already has them, so Qt's scan-authored today cannot double-count against the archive. The live-today patch adopts Electron's `dailyWithLiveToday` guard (override only when live ≥ stored) — a cold start with no anchor no longer zeroes today's row, which previously would also have poisoned a derived range.
- **Tab label semantics** follow `syncPeriodTabs`: the middle slot always shows a month-mode label (WEEK/7D/30D while the selection lives in the month slot, otherwise the `periodMonthMode` preference); it never reads TOTAL.
- **Accepted gaps vs Electron.** Electron derives fixed ranges from per-device `history.daily` and can show per-device rows when the hub serves device histories; Qt has no per-device history, so session/project/device breakdowns under a fixed range show the `periodRange.*Unavailable` note instead of content (device rows are left empty so Home never shows month data under a fixed range), and active time/peak come from the archive's per-day `activeTimeMs` — written by the Electron collector's `tokscale graph` tick. A Qt-only install shows 0m active time until a Qt-native graph tick exists.
- **Top-edge hide animates** like Electron `topEdgeHide.js`: 240ms y-animation (InCubic hiding, OutCubic revealing), 6px hoverable peek (was 4px teleport), 150ms dock debounce on cursor-leave; tray/hotkey show still restores instantly.
- **Popups stopped teleporting.** The settings slot animates height 150ms + opacity 120ms (Electron `max-height 140ms / opacity 120ms`), the footer view menu and the period menu get Electron's enter/exit transitions (190ms in, 130ms out, bottom-left origin for the view menu).
- **Settings parity of the 采集 / AI 工具额度 sections confirmed real (2026-09-19):** the collection checkboxes write the `clients` CSV and `updateSetting` restarts the usage runtime + watcher roots; the eye toggle writes `hiddenClients`; the limits list writes `limitProviders`, which `LimitsRuntime` turns into real HTTP fetches per enabled provider (24 catalog ids dispatched in `limits/Providers.cpp`, snapshot cached to `limits-snapshot.json`). Same caveat as the Limits decision above: cookie/OAuth providers read Electron-written local credentials and unmapped quota payloads surface as empty windows with `status: ok`.

## Home limits, chrome alignment, ZCode GLM lane (2026-09-19)

- **GLM missing from Home limits was a provider gap, not layout.** Electron's zai row merges two lanes: the console-key quota lane and the local **ZCode desktop login lane** (`discoverZcodeConnection()` reads `~/.zcode/v2` — setting.json family/selection, config.json provider mirror JWT, coding-plan-cache.json entitlement, telemetry-state deviceMid). Qt only had the console-key lane, so a ZCode-managed GLM read "Not signed in". Ported as `core/limits/ZcodeDiscovery.{h,cpp}`: discovery + `parseZaiUsage` (5-hour/Weekly/MCP windows, CREDIT_LIMIT treated as a token window) + `parseZcodeStartPlanBalances` (bucket grouping, earliest-boundary pick, daily-entitlement plan preference); `fetchZai` runs both lanes and merges with Electron's status logic (oauth source, empty-but-attempted → unavailable, not notConfigured). `normalizeLimitWindow` now also keeps `limitId`/`boundaryKind`/`windowMinutes`/`resetDescription`/`detail` like Electron core.js. The boundary text is formatLimitBoundary ("Reset/Expires/Changes in …"), and resetText falls back to the window's cadence label via `home.reset`. Skipped: console-key cash balance + local spend tracking (key-lane-only feature; noted gap).
- **Windows path gotcha:** this repo's own dev shell exports `ZCODE_WINDOWS_APP_INSTALL_DIR` (set by the zCode CLI), which would send the discovery chain to the install dir; the Qt chain resolves `ZCODE_DATA_BASE_DIR` → `ZCODE_WINDOWS_APP_INSTALL_DIR` → **USERPROFILE** → HOME → homePath (USERPROFILE before HOME, since a shell-exported HOME can be an MSYS `/c/…` path QFile cannot open). GUI-launched widgets never carry those vars; `src/tests/zai_probe.cpp` exists as an uncommitted scratch harness for probing the lane (add its CMake target manually if needed again).
- **Back to Home gating now matches Electron `setBreakdown`:** the row only appears after leaving Home through a home-module click (`setViewFromHome`), and any switcher/tray navigation resets it. This removes the stray 返回主页 on tool/model/device/session/project/limits.
- **Fonts:** Electron's `--ui-font` default leads with `ui-monospace`, which Chromium resolves to **Consolas** on Windows; Qt preferred Cascadia Mono (wider), which is why rows and home window cells read crowded next to Electron. Consolas now leads Qt's UI-font fallback. UsageRow centers the name block against the value/cost stack (`.row-head align-items:center`), shows the `YOU` badge uppercase, and `iconUrl()` aliases vendor-reuse ids (zcode→zai, micode→xiaomi, hermes→hermes-agent, grok→xai) so ZCode shows the Z mark instead of a dot.
- **Home limits module:** accounts sort by lowest remaining window (Electron `homeLimitAccounts`), and an empty `homeWindows` no longer shadows the full `windows` list (an empty QVariantList is truthy in QML and used to blank accounts that only had unranked windows).
- **View-switcher menu** width now tracks the switcher itself (`width: 100%` in Electron) instead of a fixed 168px panel, with items filling the popup.
- **Limits page:** the "Updated …" meta sits at the block's left edge (Electron `.limit-title` grid), vertically aligned with the window labels, not indented after the provider mark. Device rows drop the platform subtitle (Electron keeps the head one line; platform/version live in the row detail).
- **Device view under a fixed range:** Electron derives per-device rows from device histories. Qt only owns local history, so a lone (local) device derives its tokens/cost from the fixed-range period; a multi-device hub without histories shows the range note. This is narrower than Electron's hub-side device histories but never shows month data under a fixed range.
- **Trend freshness:** the live pipeline was verified end-to-end (headline moved 697,303,131 → 744,230,512 within one 30s window while usage accrued; limits refresh in the same rebuild). Electron's peak label = `max(historySummary.peakDayTokens, daily max)`; Qt's home peak = max over retained history incl. the live-today patch — same semantics. Remaining cadence differences would come from scan scheduling, not missing updates.

## Full parity audit: Electron ↔ Qt (2026-09-19)

Inventory method: every Electron `defaultSettings()` key, every settings-panel section/row (`index.html` sections general/main/window/appearance/tools/limits/subscriptions/sync), the tray menu (`tray.js buildTrayMenuTemplate`), window chrome, the nine breakdown views, Home modules, the floating bubble, top-edge hide, the dashboard window, hub/agent/sync, and export were enumerated against the Qt tree (`SettingsStore::defaults`, `SettingsPanel.qml` sections, `TrayController`, QML views). Judgments below are recorded once and govern: **对齐 = implemented this pass; 等效 = different mechanism, same user-visible result; 缺口-记录 = intentionally not ported, with the reason.**

| Surface | State | Judgment |
|---|---|---|
| Settings keys (defaultSettings ↔ SettingsStore::defaults) | 对齐 (this pass) | `windowToggleShortcut` default `''` (Electron parity; was Ctrl+Alt+M), `historyIntervalMs` 15m. Credential keys are seeded empty on the Electron side but stored in `credentials.json` via the shared-shape store on the Qt side — 等效. `floatingBubbleCustomLayout`/`floatingBubbleBounds`/`trayCustomLayout` not stored (Qt bubble/tray have no layout editor — see below). |
| Settings · 常规 | 对齐 (this pass) | language/startAtLogin/updates/discord/about already matched; added: Latest-version wiring (`appUpdateLatest`), "View release" button. Electron's release-notes pane is web-content from GitHub; the Qt row opens the release page — 等效. Electron's advanced/tokscale info row not ported (tokscale resolution is compile-time fixed in Qt; nothing user-actionable) — 缺口-记录. |
| Settings · 主画面 | 对齐 (this pass) | Added: currency **rate mode** auto/manual + manual rate field (`currencyRates[code]` override semantics copied: value present = manual, key deleted = auto), **home limit account count**, **active days window** (all/year, year counts the heatmap year). Electron's home-module order editor (drag `homeModuleOrder`) not ported; Qt keeps the default order + hidden-module support — 缺口-记录. `vendorColors` editor not ported (model/vendor colors are the shared table) — 缺口-记录. |
| Settings · 窗口 | 对齐 (this pass) | Added: **tray display** combo (writes `trayContent`), **hide app icon** (`Qt::Tool` + `WS_EX_TOOLWINDOW`; note `setFlag(Qt::Tool,false)` alone would also clear the `Qt::Window` bit — the reason a plain window regression occurred during this pass), **bubble content** (icon/Σ vs tokens). Not ported: tray-mode layout editor (`trayCustomLayout`), floating-bubble trigger `hover` (Qt bubble is an always-on companion window, not the collapsible Electron bubble — trigger is meaningless there) — 缺口-记录. Shortcut **recorder** not ported; the toggle shortcut still works via `windowToggleShortcut` (settable from Electron on a shared `settings.json`) — 缺口-记录. |
| Settings · 外观 | 对齐 (this pass) | Added: interface/display **font presets** (App default/System/Mono/Custom + custom field) writing `interfaceFontFamily`/`displayFontFamily`; UI font default now Consolas-first to match Chromium's `ui-monospace` on Windows. Glass/backdrop/live-dot/tool-icons/compact totals/rate/scope/units/settings-in-titlebar/depth/zoom/reduce-motion/theme presets already matched. Font *preview* pane not ported — 缺口-记录. |
| Settings · 采集 | 对齐 (this pass) | Added: **collection mode** (live / smart / interval — `UsageEngine::start` now watches+ticks, watches with a fixed 10m tick, or drops the watcher, matching `collectorIntervalMs`/`collectorWatchEnabled`), **collection cadence** (5/15/30m), **auto export** toggle + interval + native folder picker (`pickExportDir` → QFileDialog; Electron uses dialog.showOpenDialog) + export now/diagnostics. Session-usage-archive toggle not ported (Qt reads Electron's archive but writes none; a toggle that gates nothing would lie) — 缺口-记录. Custom-model-pricing editor not ported (`customModelPricing` stays editable from Electron/JSON on the same `settings.json`) — 缺口-记录. |
| Settings · AI 工具额度 | 对齐 (this pass) | Added: **show used** toggle, **refresh mode** (fixed/adaptive) + **interval** (1/2/5/15/30m), and condensed **per-provider credential editors** (claude cookie, deepseek/minimax/kimi/zai/zaiteam keys, copilot token+host, volcengine 4 keys, alibaba/qoder/zed/commandcode/ollama/opencode cookies, trae token+deviceId) saved through the shared credential store with a "set" presence dot — the field lists mirror `CREDENTIAL_SETTING_PATHS` in both apps. Not ported: Codex OAuth login/workspace switcher, Cursor multi-account switcher, MiMo managed accounts (login-flow UIs need browser automation; accounts stay Electron/CLI-managed on the same store), `maskLimitAccountEmails` (Qt renders no account emails to mask) — 缺口-记录. |
| Settings · 订阅信息 / 多设备同步 | 等效 | Subscriptions add/remove + hub mode cards + hub URL/device ID match; Electron's subscription *edit* and orphan-recovery prompts are not ported (records still readable) — 缺口-记录. |
| Tray menu | 对齐 (this pass) | Full `buildTrayMenuTemplate` parity: refresh, open-view submenu (all 9 views, hidden ones disabled), tray-display submenu (14 content modes), window-presentation submenu (tray/floating/normal/desktop; `tray` parks the window until the next tray click), version row, settings, quit — all localized through the shared `trayMenu.*` keys. Codex account submenu is part of the Codex login gap above. The refreshing→disabled state is not reflected while the menu is open (Windows builds menus on open; Electron rebuilds its template) — 缺口-记录. |
| Window chrome | 对齐 | Titlebar hover actions, period tabs + month menu (label WEEK/7D/30D), back-home gating, footer switcher (width = switcher, animated), settings animation, fixed-range notes, top-edge hide animation (240ms In/OutCubic, 6px peek, 150ms debounce) — all matched in the previous two passes. |
| Breakdown views | 对齐 | tool/model rows (font metrics, centered head, vendor-mark aliases), device rows (derived fixed-range row for the lone local device; platform in detail only), session/project (range notes), limits page (updated-at alignment, boundary-aware reset text), trends (range-scoped active time/peak, per-range labels), status view. |
| Home modules | 对齐 | limits (sorted by lowest remaining, count setting, GLM via the ZCode lane), tools/models (5), devices (4), activity heatmap + trend (peak = retained max ∪ live today; active-days window setting). |
| Floating bubble | 等效 | Second companion window with icon/compact-total content. No collapse/hover-open choreography — 缺口-记录 (Electron's bubble is a different window model; parity here would be a reimplementation, not an alignment). |
| Discord RPC | 缺口-记录 | The setting toggles and is stored identically, but the RPC runtime needs a librdkafka/IPC client — no third-party C++ libs is a hard plan bound. Windows-only impact. |
| DSH zstd decode, Worker rewrite, macOS WidgetKit/vibrancy | 缺口-记录 | Plan bounds unchanged (zstd needs a decompressor; the Worker stays JS; macOS targets out of scope for this Windows-first port). |
| Hub / agent / sync | 对齐 | Same wire shape (`docs/API.md`), agent.pid backoff, subscriptions versioning; `runtime: "native"` hubBuild handling recorded earlier. |
| Export | 对齐 (this pass) | CSV+JSON manual + auto-interval + diagnostics, folder picker. |
| QML module imports | 等效 | The settings folder picker uses the native `QFileDialog` because `QtQuick.Dialogs` is not part of the deployed QML set for this exe — native dialog matches Electron's behavior anyway. |

Verdict: every settings key both apps share is now reachable from the Qt UI or has an explicit judgment above; the remaining gaps are the account-login flows (browser automation), the layout editors, Discord RPC, and the plan-bounded items — none silently diverge: each is recorded here with its reason.

## Period totals, trend stats, hover menu (2026-09-19, round 2)

Root causes for "Qt totals / active time / peak day read lower than Electron", each fixed at the source:

- **Derived ranges and trend stats came from accumulated snapshots, not from the tools.** Electron rebuilds its daily history every `historyIntervalMs` (default 15m) with `tokscale graph --client <csv> --no-spinner`, whose contributions carry per-day tokens/cost/activeTimeMs plus per-client/per-model attribution; fixed ranges (本周/最近 7 天/最近 30 天) and the trend cards are derived from that. Qt only had its own daily snapshots plus Electron's on-disk archive. Ported: `TokscaleRunner::scanGraph` (positional `graph` subcommand, no `--json`/grouping), `UsageEngine` runs it on full ticks gated by `historyEnabled` + `historyIntervalMs` (forced on a local-day rollover and on a tracked-clients change; applied before `mergeHistoryDay` so today stays the freshest record), and a `parseGraphResult` port (`graphHistoryDays`: disjoint reasoning buckets for reasonix/codex/droid/dsh, `omp→pi`/`kilocode→kilo` aliases, 370-day rolling cap, days outside the graph range keep their existing records). Verified live: `history.json` now holds 23 days of full coverage with `activeTimeMs`, and 本周/最近30天/活跃时间/峰值单日 moved onto the graph data.
- **TOTAL was missing Electron's main-process display compositions.** `periods.allTime` in Electron = tokscale since-scan + Proma + Qoder-CN + WSL (Qt already had these) **+ `archivedClientUsage` + `sessionUsageArchive`** (Qt had neither). Ported as `core/usage/ArchiveUsage.{h,cpp}` applied inside `DeviceRuntime::deviceRecord()`: `applyArchivedClientUsageToRecord` (untracked clients' captured usage; today/month only while the capture day/month matches, allTime unconditional, active clients skipped, `addClientUsage` semantics), and `applySessionUsageArchiveToRecord` (retained sessions from `session-usage-archive.json`, mtime-cached read from userData/shared data dirs; `addArchivedSession`/`addSessionBreakdown` including the component split, multi-model unclassified fallback, reasonix synthetic-session guard). Verified live: TOTAL moved 779.9M → 989.6M on this machine (the session archive alone is 259KB of retained cursor sessions). `--scan-once` does not run these applies (dev path), and warm ticks do not run the graph (Electron gates graph ticks by time too; a warm-tick graph would sit on the 3–5s watch path).
- **`settings.customScanPaths` never reached tokscale.** Electron injects `TOKSCALE_EXTRA_DIRS` (each client expanded through `tokscaleCustomScanClientIds` — pi/kilo pin one concrete id so shared-parser roots are not read twice). Ported: `Subprocess::runProcess` takes extra env, `TokscaleRunner` carries the env for every scan, `Catalog::tokscaleExtraDirsEnv` builds it. Empty on this machine, so it did not contribute here, but it was a silent under-count for anyone using custom roots.
- **Home limits label alignment:** the window-cell row lost 12px to its own `RowLayout` spacing (18px spacer + 12px spacing = 30 vs the account name at 18). Spacer is now 6 so "Cursor Models" starts exactly under the "C" of "Cursor" (Electron `margin-left: 18px`).
- **View-switcher hover:** Electron opens the menu on the disclosure's `pointerenter` and closes 160ms (`VIEW_SWITCHER_HOVER_CLOSE_MS`) after the pointer leaves the whole switcher. Ported in `Footer.qml`: `containsMouse` on the chevron opens immediately; a 160ms timer closes it unless the pointer is over the popup, the switcher, or the current-view button; the click toggle still works.

## Hover-menu keep-open, range-aware trend bars (2026-09-19, round 3)

- **Moving straight up from the chevron closed the list.** The 160ms hover-close timer was armed when the pointer left the chevron, and the keep-open check relied on `menu.hovered` — which never fires because QQuickPopup is not a Control and has **no** `hoverEnabled` property (assigning one is a load failure, caught by the temporary load diagnostics). The popup now carries a background `MouseArea (acceptedButtons: NoButton, hoverEnabled: true)` whose `containsMouse` stops/rearms the close timer, exactly matching Electron's "leaving the whole switcher closes it, moving inside never does".
- **Trend bars now follow the selected period's actual usage.** Electron deliberately keeps the long-range chart under fixed ranges (user judged that wrong): 本周/最近 7 天/最近 30 天 show their own daily bars (zero-filled across the fixed range), MONTH shows the calendar month's daily bars, TOTAL shows the monthly rollup (last ~13 months, Electron's monthly series), DAY keeps the trailing 7-day context. Bar gaps shrink adaptively so 30 bars keep their 6px minimum width; date labels render yyyy-MM as "N月". Recorded as a deliberate user-requested divergence from Electron's chart behavior — the stat cards underneath keep the Electron activityStatsForPeriod semantics.

## Trend bar hover tooltip (2026-09-19)

The trends bars carry the Home activity heatmap's hover treatment: a dark rounded card (same color/border/type sizes) pinned above the hovered bar showing `formatTokens(tokens) + " tokens"` and the bar's date (ISO day for daily ranges, `yyyy-MM` on TOTAL's monthly rollup). Layout math (`slotGap`/`barWidth`) is shared between the bar Row and the tip so the card centers on the hovered bar and clamps to the chart edges; the hovered bar also brightens. Hover merged into the existing spark MouseArea, so click-to-dashboard still works.

Tooltip text is center-aligned in both the trend bar card and the Home heatmap card (each line anchored to the column's horizontal center — binding the lines to the column's width would loop through the positioner's childrenRect and zero the layout, which is why the first attempt collapsed the card).

The trend tooltip triggers only on the bar itself: `onPositionChanged` hit-tests the pointer against the bar's visible rectangle (centered 72% slot width, below the bar top, tokens > 0) — empty chart space, slot gaps, and zero-day baselines show nothing.

## Windows packaging: installer, portable ZIP, zero-residue uninstall (2026-09-19)

The Qt CI job used to upload a loose `windeployqt` dump of `qt/build`, and that artifact could not work on a clean machine. Three gaps, all silent at runtime: no `sqldrivers/qsqlite.dll` (Cursor's `state.vscdb` token fallback and Qoder CN's `local.db` usage just return nothing), no MSVC runtime (`windeployqt` writes a 25 MB `vc_redist.x64.exe` instead of the CRT DLLs, and the `TokenMonitor*.exe` upload glob skipped even that, so the exe fails to start), and no `tokscale.exe` — the widget has no self-download path, so `Paths::tokscaleBinary()` reported `tokscale_missing` forever while the artifact did not even carry `download-tokscale.ps1`. Judgments:

- **One packaging entry point, shared by the local machine and CI.** `scripts/package.ps1` builds (`build.ps1`), deploys (`windeploy.ps1`), stages, asserts, smoke tests, zips and compiles the installer; CI calls it with `-SkipBuild -SkipChecks` after its own build step. `scripts/env.ps1` was made environment-agnostic for this (`QTDIR`/`QT_ROOT_DIR`/`CMAKE_PREFIX_PATH` → `qmake` on PATH → the local kit; `vcvars.ps1` only when `cl.exe` is missing), which is why CI no longer needs to re-derive the configure command by hand.
- **The stage mirrors `qt/build` by whitelist, not `windeployqt --dir`.** The deployed build directory is the layout this port has been running from for weeks; a second deployment mode would have been an unverified variable. Instead the whitelist is what it is, and `package.ps1` asserts the files a missing piece would silently break (`qwindows`, `qschannelbackend`, `qsvgicon`, `qsqlite`, CRT, `tokscale.exe`, the Basic Controls style). That assertion is also what would have caught the CI `sqldrivers` hole.
- **App-local MSVC runtime instead of `vc_redist.x64.exe`.** The installer is per-user (`RequestExecutionLevel user`, `%LOCALAPPDATA%\Programs`), and running the redistributable needs elevation; Microsoft documents local deployment of `Microsoft.VC143.CRT` as the alternative. Trade-off accepted: those DLLs are not serviced by Windows Update, so a CRT security fix means rebuilding. The old upload shipped neither, which is worse than either choice.
- **`tokscale.exe` is bundled beside the exe and packaging fails without it.** Same pinned fork asset and sha256 as `scripts/vendor/tokscale.json`; `-TokscalePath` wins, then a hash-matching copy (e.g. the one `download-tokscale.ps1` already installed in userData), then the pinned release. A package that cannot scan usage is not a deliverable, so there is no degraded mode. `%APPDATA%\Token Monitor\tokscale.exe` still wins at runtime (`Paths::tokscaleBinary()`), which is deliberate: that copy is the user's.
- **The installer is NSIS, matching the Electron build** (`package.json` ships an NSIS setup + portable exe), with `makensis` located by path — `-MakensisPath`, `PATH`, `%ProgramFiles(x86)%\NSIS`, the chocolatey shim, then electron-builder's cached NSIS. That last one is why a local installer costs no extra download; CI installs NSIS with `choco install nsis` because `windows-latest` (windows-2025) does not preinstall it, and a PATH-independent lookup avoids depending on whether a mid-job machine PATH change is visible to later steps.
- **Version comes from `tmon.h` (`kAppVersion`).** `CMakeLists.txt`, `tmon.h` and `package.json` all carry the number; the package name follows the value the app itself reports, and a disagreement with `CMakeLists.txt` is a warning rather than a hard failure (the two have always agreed, and a hard failure would add a third place to remember).
- **The uninstall contract is "nothing left behind", and the shared directory is the one exception that asks.** The widget writes to three places: `%APPDATA%\Token Monitor` (shared with Electron — `settings.json`, `credentials.json`, `history.json`, `collector-anchor.json`, `agent.pid`, `hub-devices.json` exist in both), `%LOCALAPPDATA%\Token Monitor\cache` (QML disk cache + pipeline cache) and `%LOCALAPPDATA%\Javis\Token Monitor\cache` (pipeline cache, `organizationName`). Qt-only names inside the shared directory — `limits-snapshot.json`, `data\` (the standalone hub's `devices.json` and the spend stores), `qt-*.json`/`qt-*.log` (left over from earlier iterations; nothing in the current tree writes them), and the root `tokscale.exe` — are always removed. The directory as a whole is removed when no Electron trace is found, when the user says Yes, or on a silent uninstall; `Uninstall.exe /S /KEEPDATA` is the conservative path, interactive runs ask (default Yes) with the consequence spelled out, and the uninstaller reports any residue it can still see instead of assuming success. Electron's own `Run\com.javis.tokenmonitor` login item and its Chromium/archive files are never touched on their own.
- **`startAtLogin` now writes a quoted Run value.** The default install path contains a space (`...\Programs\Token Monitor Qt\TokenMonitorQt.exe`) and Windows' Run-key parser splits an unquoted path, so the app's own toggle would have written an entry that never starts. The installer writes the quoted form, so both writers now agree.
- **The widget carries an icon** (`resources/app.ico` + `resources/app.rc`, `enable_language(RC)` on Windows): the exe had no icon resource at all, so the taskbar, Start menu and the ARPP entry all fell back to the generic Windows icon. The NSIS installer, the shortcuts and the uninstall entry reuse the same `.ico`.

Verification lives in `scripts/verify-install.ps1` (`package.ps1 -VerifyInstall`, and CI runs it): it refuses to run while a Token Monitor process is alive, backs up the three directories with a SHA256 manifest plus every Run value, the uninstall key and the shortcuts, then does portable-extract → install → smoke → uninstall → residue assertions, then `/S /KEEPDATA`, restoring and re-hashing after each pass so the destructive test cannot leave the machine changed.

Follow-up from the first real run of that verification: the `/KEEPDATA` branch was broken, and `Uninstall.exe /S /KEEPDATA` deleted the shared directory anyway. Instrumenting a copy of the script (a temp `USER_DATA_DIR` plus a `FileWrite` of the four decision flags) showed why: FileFunc's `${GetOptions}` returns "" even for a literal `"/S /KEEPDATA"` needle on NSIS 3.0.x, while `${GetParameters}` returns `/S /KEEPDATA` as expected — and the uninstaller never sees the original command line verbatim, because NSIS re-launches itself from a temp copy as `"<temp>\Un_A.exe"  /S /KEEPDATA` (double space). The flag is now read with a plain case-insensitive scan of `${GetParameters}`. Three related NSIS traps are worth keeping: `StrCmp` is already case-insensitive (`StrCmpS` is the case-sensitive form; there is no `StrCmpI`), `SetShellVarContext` / `SetRegView` are only valid inside a Section or Function, and the script must stay ASCII-only (NSIS 3.0.x decodes it with the system code page and aborts with "Bad text encoding" on UTF-8 comments). The pass that caught this — seed a Chromium marker and a `qt-*` probe, uninstall with `/KEEPDATA`, assert the marker survived and the probe did not — is therefore not optional: it is the only thing standing between "silent uninstall deletes everything" and "a user's Electron configuration is gone".

## Autostart ownership and the top-edge crash (2026-09-20)

Two defects reported against the 0.57 build downloaded from CI, each traced to a specific line rather than guessed at:

- **The widget started at login although nobody enabled it.** `HKCU\...\CurrentVersion\Run\TokenMonitor` pointed at the installed exe (`D:\App\TokenMonitorQt\TokenMonitorQt.exe`) while `settings.json` said `startAtLogin: false`, so the widget's own switch showed "off" and could not explain the behaviour. Cause: the installer's `Section "Start with Windows"` had no `/o` flag and NSIS selects sections by default, so the checkbox was ticked in every install — a second writer of a value the widget already owns. The Electron build has no installer component for autostart at all: it owns the feature (`applyLoginItem`, `main.js:3085`) and syncs its setting from the OS (`syncLoginItemSettingFromOs`, `main.js:3092`). Ported that: the component is gone, `AppState::syncStartAtLoginFromOs()` adopts the registry state into settings at startup — deliberately without going through `updateSetting()`, which would rewrite the value with this copy's path and repoint another copy's entry — and the uninstaller deletes the value only when it points into `$INSTDIR`, because an entry pointing elsewhere belongs to a portable build or a second install. The residue audit was relaxed to match, otherwise it would warn about an entry it intentionally kept.
- **Dock → reveal killed the process.** Four APPCRASH records with the same signature: `Qt6Core.dll`, `0xc0000005`, offset `0x20BBC4`. `dumpbin /exports` puts `?stop@QAbstractAnimation@@QEAAXXZ` at `0x20BBC0`, so the fault is inside `QAbstractAnimation::stop()`. `WidgetWindow::animateWindowY()` starts the animation with `DeleteWhenStopped` while `m_yAnimation` was a raw pointer: the 240 ms hide animation finished, Qt freed it, the pointer dangled, and the reveal's `stopYAnimation()` called `stop()` on freed memory. Both `m_yAnimation` and `m_window` are `QPointer`s now, and `stopYAnimation()` has one deletion path (`DeleteWhenStopped`; the extra `deleteLater()` was the second one and is what made the dangling handle look alive).

The crash itself depends on the allocator reusing the freed block — on a quiet preview-mode run the freed memory was still readable, so `stop()` silently "worked" and the first version of the self-test passed on the unfixed build. The regression test therefore asserts the *invariant* rather than the crash: `TokenMonitorQt.exe --self-test-top-edge` drives three dock/reveal cycles with no mouse and, ~500 ms after each leg (the animation is 240 ms), requires that no handle to a finished animation survives; otherwise it prints `top-edge-stale-animation` and exits 1. Red/green was recorded on this machine: unfixed → `top-edge-stale-animation at leg 1`, exit 1; fixed → `top-edge-ok`, exit 0, 13 consecutive runs. It runs inside `smoke-test.ps1`, so packaging and CI exercise the crashing path from now on. Nothing in CI moves the real mouse, which would be flaky.

Also fixed while in there: the exe carries a version resource again, generated from `tmon.h` (`resources/app.rc.in` → `app.rc` at configure time), because the crash reports we had to work from said `TokenMonitorQt.exe, version 0.0.0.0` — which is what an exe without a version resource reports to Event Viewer, Task Manager and WER.

## The verification script emptied the machine's Run key (2026-09-20)

The sentinel assertion failed twice with `'TokenMonitorVerifySentinel' is now ''` and `State at failure: the Run key exists but is empty`. It named the wrong suspect both times, and the same bug was behind two earlier reports: the first real-machine run's `Uninstall disturbed the Electron login item 'com.javis.tokenmonitor'`, and this machine's Run key being empty (Docker Desktop, AMDNoiseSuppression, Steam, MicrosoftEdgeAutoLaunch all gone) while `StartupApproved\Run` still listed the entries — which was written off as an external startup cleaner. That attribution was wrong, and the value that disappeared was deleted by this script.

Cause: `New-Item -Path <registry key> -Force` is not "make sure this key exists". On a key that already exists it recreates the key, and recreating a key drops every value in it. The script used that form at the three Run-key sites — seeding the sentinel, seeding the entry before the uninstall call, seeding the `/KEEPDATA` pass — so the first seeding deleted every other program's autostart entry, and the second deleted the sentinel itself. That is why the failure surfaced one step later, at `Assert-SentinelIntact 'uninstaller'`, where only the uninstaller had run: the value was already gone before it started. Mechanical proof: a scratch key holding three values keeps all three across `New-Item` (guarded by `Test-Path`) and loses all three to `New-Item -Force`.

What settled it, after a manual replica of the same sequence (install → smoke → uninstall, with foreign values planted) left every probe intact: a 250 ms watcher on the key logged every transition, and both wipes line up with the script's own seeding statements at a moment when no Token Monitor process was alive. Nothing was deleted during the installer, the widget smoke test or the uninstaller — the three steps the assertion had been blaming.

- `Ensure-RunKey` creates the key only when `Test-Path` says it is missing, and no registry path in the file is ever passed `-Force` (the environment preflight now writes a probe *value* instead, which is the access a run actually needs).
- `Assert-ForeignRunNamesPreserved` snapshots the names of other programs' values before each seeding step and fails the run at that step, naming what disappeared. The snapshot must be taken before the key is created: taking it after `Ensure-RunKey` left the guard blind to exactly this bug, which is what the red test showed.
- `Backup-State` records every Run value — name and command line — in `state.json`. They are deliberately not restored (other programs own their entries, and the Electron build rewrites its own on start), but a value lost during a run can now be named and re-added; the restore step warns with the recorded command lines. When this bug hit, no such record existed, which is why the deleted entries could not be recovered.
- The sentinel message no longer opens with "an external cleanup"; when the key is empty it points at the script's own seeding steps first.

Red/green on this machine: a copy of the script with `Ensure-RunKey` reverted to `New-Item -Path $RunKeyPath -Force` fails at the seeding step with `This script's own 'seed' step removed Run value(s) belonging to other programs: AnotherAppEntry, UnrelatedProbe`, exit 1, before the installer runs; the fixed script passes all three passes with a foreign value planted, and the watcher log shows that value surviving every step, including both uninstall passes.

Recovery is the apps' own settings: each program rewrites its Run value when its "start with Windows" option is applied again, and the snapshot in `state.json` gives the exact command line for anything that has to be re-added by hand. Two probe entries planted during this investigation were deleted by the reproduction runs, and the four real entries on this machine were already lost to the first run — before the snapshot existed.


